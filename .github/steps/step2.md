# WebAssembly Security

## Paso 2 de 10: Análisis con wabt

Crea `tools/wabt-scan.sh` que ejecute `wasm-objdump -x src/hello.wasm`.

### Tu tarea

Crea `tools/wabt-scan.sh` que ejecute `wasm-objdump -x src/hello.wasm`.

---
*Paso 2 de 10 — completa la tarea y haz commit para continuar.*
