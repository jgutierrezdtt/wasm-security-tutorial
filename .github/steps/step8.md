# WebAssembly Security

## Paso 8 de 10: Content-Security-Policy para WASM

Crea `src/csp-headers.json` con `script-src 'wasm-unsafe-eval'` correctamente.

### Tu tarea

Crea `src/csp-headers.json` con `script-src 'wasm-unsafe-eval'` correctamente.

---
*Paso 8 de 10 — completa la tarea y haz commit para continuar.*
